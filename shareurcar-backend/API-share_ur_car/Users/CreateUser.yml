info:
  name: CreateUser
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/users
  body:
    type: json
    data: |-
      {
        "firstname": "Javier",
        "lastname": "Rodríguez",
        "email": "javi@test.com",
        "password": "1234"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
